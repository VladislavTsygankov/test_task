const BY_NAME = "country name";
const BY_FULL_NAME = "full country name";
const BY_CURRENCY_CODE = "country currency code";
const BY_CODE = "country code";

export {
  BY_CODE,
  BY_CURRENCY_CODE,
  BY_FULL_NAME,
  BY_NAME,
}